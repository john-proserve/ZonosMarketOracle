import styled from "styled-components";

export const Card = styled.div`
  background: white;
  border-radius: 6px;
  box-shadow: inset 0px -1px 1px 1px rgba(0, 0, 0, 0.08);
  filter: drop-shadow(0px 1px 3px rgba(110, 110, 120, 0.08));
  padding: var(--amino-space-24);
  display: block;
`;
